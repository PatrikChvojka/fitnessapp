import 'package:flutter/material.dart';

class MainAppStyle {
  // font family
  ThemeData themeData = ThemeData(
    fontFamily: 'Raleway',
  );

  // MAIN Color
  Color mainColor = Color.fromRGBO(77, 183, 74, 1);
  Color secondColor = Color.fromRGBO(17, 40, 74, 1);

  // bodyBG
  Color bodyBG = Color.fromRGBO(255, 255, 255, 1);

  // buttonBG
  Color buttonBG = Color.fromRGBO(77, 183, 74, 1);
  Color buttonTextColor = Colors.white;
}
